import { Given, When, Then } from "@badeball/cypress-cucumber-preprocessor";

Given('o usuário está logado com o email {string} e senha {string}', (admin: string, password: string) => {
});


When('Eu crio uma categoria com o nome {string}', (categoryName: string) => {

});

Then('Eu vejo a categoria com o nome {string} na lista de categorias', (categoryName: string) => {

});
